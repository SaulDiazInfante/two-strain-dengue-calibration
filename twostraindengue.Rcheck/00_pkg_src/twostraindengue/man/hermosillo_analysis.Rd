% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hermosillo_analysis.R
\name{hermosillo_analysis}
\alias{hermosillo_analysis}
\alias{plot_observed_cases}
\alias{plot_model_fit}
\alias{plot_likelihood_surface}
\alias{plot_profile_likelihood}
\alias{run_hermosillo_2010_analysis}
\title{Hermosillo 2010 Analysis Workflow}
\usage{
plot_observed_cases(
  data,
  rows = seq_len(nrow(data)),
  title = "Observed Cases",
  ylim = NULL
)

plot_model_fit(
  model_output,
  data,
  rows = seq_len(nrow(data)),
  title = "Model Fit",
  week_origin = min(data$week[rows]),
  xlim = NULL,
  ylim = NULL
)

plot_likelihood_surface(
  x_values,
  y_values,
  surface,
  x_label,
  y_label,
  point = NULL,
  label = NULL,
  main = "Likelihood Contours"
)

plot_profile_likelihood(
  values,
  relative_profile,
  x_label,
  interval = c(lower = NA_real_, upper = NA_real_),
  interval_label = NULL,
  main = "Relative Profile Likelihood"
)

run_hermosillo_2010_analysis(
  data = NULL,
  observed_rows = 1:6,
  full_time_grid = seq(0, 18, by = 0.1),
  calibration_grid = seq(0, 6, by = 0.1),
  fit_time_grid = NULL,
  beta_h_values = seq(1, 3.5, length.out = 250),
  beta_m_values = seq(0.001, 0.0175, length.out = 250),
  r01_values = seq(1.65, 3, length.out = 250),
  r02_values = seq(0.003, 0.006, length.out = 250),
  r0_values = seq(1.25, 1.75, length.out = 250),
  output_dir = "outputs",
  timestamp = NULL,
  write_outputs = TRUE,
  optim_start = c(0.9, 0.004)
)
}
\arguments{
\item{data}{Study data used for calibration and plotting.}

\item{rows}{Integer row indices to display.}

\item{title}{Plot title.}

\item{ylim}{Optional y-axis limits.}

\item{model_output}{Matrix or data frame returned by \code{\link[=run_model]{run_model()}}.}

\item{week_origin}{Week number corresponding to simulation time zero.}

\item{xlim}{Optional x-axis limits.}

\item{x_values}{Numeric vector for the x-axis.}

\item{y_values}{Numeric vector for the y-axis.}

\item{surface}{Numeric matrix of likelihood values.}

\item{x_label}{X-axis label.}

\item{y_label}{Y-axis label.}

\item{point}{Optional numeric vector of length 2 to highlight on the plot.}

\item{label}{Optional legend label for \code{point}.}

\item{main}{Plot title.}

\item{values}{Numeric vector defining the profile grid.}

\item{relative_profile}{Numeric vector of relative profile likelihood values.}

\item{interval}{Named numeric vector with \code{lower} and \code{upper}.}

\item{interval_label}{Optional character vector for the interval legend.}

\item{observed_rows}{Integer row indices defining the calibration window.}

\item{full_time_grid}{Numeric solver grid used for the baseline simulation.}

\item{calibration_grid}{Numeric solver grid used during likelihood
evaluation.}

\item{fit_time_grid}{Optional solver grid used for the fitted trajectory. If
\code{NULL}, a grid covering the observed study window is built automatically.}

\item{beta_h_values}{Grid of \code{betaH} values for the original-scale surface.}

\item{beta_m_values}{Grid of \code{betaM} values for the original-scale surface.}

\item{r01_values}{Grid of \code{R01} values for the reparameterised surface.}

\item{r02_values}{Grid of \code{R02} values for the reparameterised surface.}

\item{r0_values}{Grid of \code{R0} values for the reparameterised surface.}

\item{output_dir}{Directory where figures and calibration tables are written.}

\item{timestamp}{Timestamp prefix used in output file names.}

\item{write_outputs}{Logical; if \code{TRUE}, writes figures and CSV surfaces to
\code{output_dir}.}

\item{optim_start}{Starting values for the constrained optimisation of
\code{betaH} and \code{betaM}.}
}
\value{
Invisibly returns the plotted subset of \code{data}.

Invisibly returns the fitted case matrix used in the overlay.

Invisibly returns \code{surface}.

Invisibly returns \code{relative_profile}.

A named list containing the fitted parameters, likelihood surfaces,
profile summaries, reproduction-number estimates, and any generated output
file paths.
}
\description{
These functions expose plotting helpers and a reusable analysis runner for
the Hermosillo 2010 case study bundled with the package.
}
\examples{
analysis <- run_hermosillo_2010_analysis(
  observed_rows = 1:3,
  full_time_grid = seq(0, 5, by = 0.5),
  calibration_grid = seq(0, 2, by = 0.5),
  fit_time_grid = seq(0, 5, by = 0.5),
  beta_h_values = seq(1, 1.4, length.out = 3),
  beta_m_values = seq(0.001, 0.003, length.out = 3),
  r01_values = seq(1.7, 1.9, length.out = 3),
  r02_values = seq(0.003, 0.0034, length.out = 3),
  r0_values = seq(1.3, 1.4, length.out = 3),
  write_outputs = FALSE
)

analysis$reproduction_numbers
}
