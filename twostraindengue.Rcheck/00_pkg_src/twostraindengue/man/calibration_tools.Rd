% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_utils.R
\name{calibration_tools}
\alias{calibration_tools}
\alias{evaluate_negative_loglikelihood}
\alias{compute_reproduction_components}
\alias{beta_m_from_reproduction}
\alias{evaluate_reparameterised_loglikelihood}
\alias{evaluate_loglikelihood_grid}
\alias{relative_likelihood}
\alias{profile_interval}
\title{Calibration Utilities}
\usage{
evaluate_negative_loglikelihood(
  beta_h,
  beta_m,
  observation_times,
  observed_cases,
  initial_state,
  time_grid,
  fixed_parameters
)

compute_reproduction_components(initial_state, fixed_parameters)

beta_m_from_reproduction(
  beta_h,
  reproduction_value,
  mode,
  initial_state,
  fixed_parameters
)

evaluate_reparameterised_loglikelihood(
  beta_h,
  reproduction_value,
  mode,
  observation_times,
  observed_cases,
  initial_state,
  time_grid,
  fixed_parameters
)

evaluate_loglikelihood_grid(x_values, y_values, evaluator)

relative_likelihood(loglikelihood)

profile_interval(values, relative_profile, cutoff = 0.146)
}
\arguments{
\item{beta_h}{Human-to-mosquito transmission coefficient.}

\item{beta_m}{Mosquito-to-human transmission coefficient.}

\item{observation_times}{Observation times on the simulation grid.}

\item{observed_cases}{Two-column matrix of observed classical and
hemorrhagic case counts.}

\item{initial_state}{Named numeric state vector used by \code{\link[=run_model]{run_model()}}.}

\item{time_grid}{Numeric vector of solver times.}

\item{fixed_parameters}{Named numeric vector containing the fixed model
parameters.}

\item{reproduction_value}{Value of \code{R01}, \code{R02}, or \code{R0}, depending on
\code{mode}.}

\item{mode}{Character scalar selecting \code{"R01"}, \code{"R02"}, or \code{"R0"}.}

\item{x_values}{Numeric vector for the first parameter axis.}

\item{y_values}{Numeric vector for the second parameter axis.}

\item{evaluator}{Function taking a two-element numeric vector and returning a
negative log-likelihood.}

\item{loglikelihood}{Numeric vector or matrix of log-likelihood values.}

\item{values}{Numeric vector defining the profile grid.}

\item{relative_profile}{Numeric vector of relative profile likelihood values.}

\item{cutoff}{Relative-likelihood cutoff used to define the interval.}
}
\value{
A scalar negative Poisson log-likelihood.

A named numeric vector with the reproduction-number components
\code{C1}, \code{C2}, and \code{C3}.

The implied mosquito-to-human transmission coefficient \code{betaM}.

A scalar negative Poisson log-likelihood after converting the
reproduction-number parameterisation back to \code{betaM}.

A matrix of log-likelihood values on the supplied grid.

Relative likelihood values normalised by the maximum entry.

A named numeric vector with entries \code{lower} and \code{upper}.
}
\description{
These helpers support likelihood evaluation, reparameterisation by
reproduction numbers, likelihood surfaces, and profile-likelihood summaries.
}
