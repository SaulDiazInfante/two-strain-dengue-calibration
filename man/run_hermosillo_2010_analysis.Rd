% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_hermosillo_2010_analysis.R
\name{run_hermosillo_2010_analysis}
\alias{run_hermosillo_2010_analysis}
\title{Run the Hermosillo 2010 Analysis Workflow}
\usage{
run_hermosillo_2010_analysis(
  config = NULL,
  config_path = NULL,
  data = NULL,
  ...
)
}
\arguments{
\item{config}{Optional named list with nested sections such as \code{data},
\code{observed_rows}, \code{grids}, \code{outputs}, and \code{optimisation}.}

\item{config_path}{Optional path to a JSON file containing configuration
overrides merged on top of the packaged defaults.}

\item{data}{Optional study data overriding the data source specified in the
config.}

\item{...}{Optional legacy flat arguments from the previous interface. These
are converted into config overrides for backwards compatibility.}
}
\value{
A named list containing the normalized config, fitted parameters,
likelihood surfaces, profile summaries, reproduction-number estimates,
summary data frames, and any generated output file paths.
}
\description{
Runs the full calibration and visualization workflow for the Hermosillo 2010
dengue case study using a compact nested configuration. The workflow reads
defaults from the packaged JSON config, supports JSON override files, and
also accepts in-memory config overrides for quick experiments.
}
\examples{
analysis <- run_hermosillo_2010_analysis(
  config = list(
    observed_rows = 1:3,
    grids = list(
      time = list(
        full = seq(0, 5, by = 0.5),
        calibration = seq(0, 2, by = 0.5),
        fit = seq(0, 5, by = 0.5)
      ),
      parameters = list(
        beta_h = seq(1, 1.4, length.out = 3),
        beta_m = seq(0.001, 0.003, length.out = 3),
        r01 = seq(1.7, 1.9, length.out = 3),
        r02 = seq(0.003, 0.0034, length.out = 3),
        r0 = seq(1.3, 1.4, length.out = 3)
      )
    ),
    outputs = list(write_outputs = FALSE, show_progress = FALSE)
  )
)

analysis$reproduction_numbers
}
\seealso{
Other hermosillo_analysis: 
\code{\link{load_hermosillo_2010_analysis_config}()},
\code{\link{plot_likelihood_surface}()},
\code{\link{plot_model_fit}()},
\code{\link{plot_observed_cases}()},
\code{\link{plot_profile_likelihood}()}
}
\concept{hermosillo_analysis}
