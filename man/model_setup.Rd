% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_utils.R
\name{model_setup}
\alias{model_setup}
\alias{default_parameters}
\alias{default_state}
\alias{load_study_data}
\alias{run_model}
\alias{extract_reported_cases}
\title{Model Defaults and Simulation Helpers}
\usage{
default_parameters()

default_state(parameters = default_parameters())

load_study_data(
  path = system.file("extdata", "raw", "study_data_dengue_hemorrhagic_fever.csv", package
    = "twostraindengue"),
  rows = 35:50
)

run_model(
  initial_state,
  time_grid,
  parameters,
  ode_function = dengue_model_ode
)

extract_reported_cases(model_output)
}
\arguments{
\item{parameters}{Named numeric parameter vector.}

\item{path}{Path to the raw study CSV. By default, this resolves to the file
shipped under \verb{inst/extdata/raw/}.}

\item{rows}{Integer row indices to select from the raw study data.}

\item{initial_state}{Named numeric state vector passed to \code{\link[deSolve:ode]{deSolve::ode()}}.}

\item{time_grid}{Numeric vector of solver times.}

\item{ode_function}{ODE callback used by \code{\link[deSolve:ode]{deSolve::ode()}}. Defaults to the
package's internal two-strain dengue system.}

\item{model_output}{Matrix or data frame returned by \code{\link[=run_model]{run_model()}}.}
}
\value{
A named numeric vector containing the default model parameters.

A named numeric vector containing the default initial state for the
ODE system, including the cumulative reported-case state \code{z}.

A data frame with columns \code{week}, \code{classical}, and \code{hemorrhagic}.

A matrix of ODE output as returned by \code{\link[deSolve:ode]{deSolve::ode()}}.

A two-column matrix with \code{classical} and \code{hemorrhagic} reported-case
trajectories extracted from the ODE output.
}
\description{
These functions expose the default parameterisation, initial conditions, raw
Hermosillo study data loader, and the ODE simulation workflow used
throughout the package.
}
\examples{
parameters <- default_parameters()
state <- default_state(parameters)
time_grid <- seq(0, 1, by = 0.1)

model_output <- run_model(state, time_grid, parameters)
head(extract_reported_cases(model_output))
}
